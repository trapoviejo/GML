program mijuego;
vars int: vidas, total;
	 float: puntos;

mapsize [256,78];


{


	vidas = 50;
    puntos = 100;
    total = puntos * vidas + puntos;
    if(total>vidas && total>puntos){
	    vidas = vidas * 2;
	}
	total = total + total * vidas;    

}
