program mijuego;
vars int: vidas, a, b;
	 float: puntos, total;
	 boolean: prueba;

mapsize [256,78];


{
    total = puntos * vidas + puntos;
    if(total>vidas && prueba && (total<(a+b+(b*(b/a))))){
	    vidas = vidas * 2;
	}
	total = total + total * vidas;    

}
